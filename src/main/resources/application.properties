# Server Configuration
server.port=8080

# Supabase PostgreSQL Configuration (for production)
spring.datasource.url=jdbc:postgresql://db.xbcgrpqiibicestnhytt.supabase.co:5432/postgres
spring.datasource.username=${SUPABASE_DB_USERNAME:postgres}
spring.datasource.password=${SUPABASE_DB_PASSWORD:your_supabase_password}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Alternative: H2 Database Configuration (for development only)
# spring.datasource.url=jdbc:h2:mem:transformerdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# PostgreSQL Configuration (alternative local setup)
# spring.datasource.url=jdbc:postgresql://localhost:5432/transformer_management
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# MySQL Configuration (alternative database option)
# spring.datasource.url=jdbc:mysql://localhost:3306/transformer_management
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (for development)
spring.h2.console.enabled=false

# Cloudinary Configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dtyjmwyrp}
cloudinary.api-key=${CLOUDINARY_API_KEY:619824242791553}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:l8hHU1GIg1FJ8rDgvHd4Sf7BWMk}

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# JSON Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
